# P2-B2: Cloudflare Worker for Backblaze B2

一个高性能的 Cloudflare Worker，用于通过 Cloudflare 安全访问私有 Backblaze B2 存储桶。支持文件代理、认证、缓存和防盗链功能。

## 功能特性

- **多种认证方式**: 支持 Clerk JWT 认证和签名 URL
- **CORS 跨域访问**: 完整的跨域资源共享支持
- **速率限制**: 基于 IP 的请求频率控制
- **防盗链**: 基于 Referer 的反扫描保护
- **Range 流媒体**: 支持视频音频流式传输
- **缓存集成**: Cloudflare Cache API 集成
- **热门文件预加载**: 自动预热热门文件缓存

## 项目结构

```
src/
├── index.ts      # 主入口，请求处理流程
├── types.ts      # TypeScript 类型定义
├── config.ts     # 配置管理模块
├── auth.ts       # 认证模块 (Clerk + 签名URL)
├── cors.ts       # CORS 跨域处理
├── rate-limit.ts # 速率限制
├── anti-scan.ts  # 防盗链
├── cache.ts      # 缓存和预加载
└── proxy.ts      # 核心代理逻辑
```

## 快速开始

### 安装依赖

```bash
yarn install
```

### 开发模式

```bash
yarn dev
```

### 部署

```bash
yarn deploy
```

## 配置

在 `wrangler.toml` 中配置以下变量：

| 变量 | 描述 | 必填 |
|------|------|------|
| `BUCKET_NAME` | B2 存储桶名称 | 是 |
| `B2_ENDPOINT` | S3 端点地址 | 是 |
| `B2_APPLICATION_KEY_ID` | 应用 Key ID | 是 |
| `B2_APPLICATION_KEY` | 应用 Key（secret） | 是 |
| `CDN_SIGNING_SECRET` | 签名 URL 密钥 | 是 |

### 可选配置

| 变量 | 描述 | 默认值 |
|------|------|--------|
| `CLERK_AUTH_ENABLED` | 启用 Clerk 认证 | true |
| `CLERK_JWT_KEY` | Clerk JWT 验证密钥 | - |
| `RCLONE_DOWNLOAD` | Rclone 下载模式 | false |
| `RATE_LIMIT_REQUESTS` | 速率限制次数 | 100 |
| `RATE_LIMIT_WINDOW` | 速率限制窗口（秒） | 60 |
| `ANTI_SCAN_ENABLED` | 启用防盗链 | false |
| `BLOCKED_REFERRERS` | 阻止的 Referer 列表 | - |
| `HOT_FILES` | 热门文件列表（逗号分隔） | - |

### 路由模式

支持三种路由模式：

1. **默认模式**: `BUCKET_NAME = "my-bucket"`
   - 所有请求指向指定存储桶

2. **$path 模式**: `BUCKET_NAME = "$path"`
   - 使用 URL 路径第一段作为存储桶名
   - 例: `https://domain.com/bucket-name/file.mp4`

3. **$host 模式**: `BUCKET_NAME = "$host"`
   - 使用子域名作为存储桶名
   - 例: `https://bucket-name.domain.com/file.mp4`

## 认证

支持两种认证方式，可通过 `CLERK_AUTH_ENABLED` 配置：

### 方式一：签名 URL（推荐）

无需额外配置，使用 `CDN_SIGNING_SECRET` 生成签名 URL：

```
exp=1699999999&sig=xxxxx
```

参数说明：
- `exp`: 过期时间戳（Unix 秒）
- `sig`: 签名

### 方式二：Clerk JWT Token

需要设置 `CLERK_AUTH_ENABLED=true` 和 `CLERK_JWT_KEY`：

```
Authorization: Bearer <clerk_jwt_token>
```

### 认证优先级

1. 签名 URL（优先）
2. Clerk JWT Token（如果启用）

## API

### 请求

```
GET /<path-to-file>
HEAD /<path-to-file>
```

### 响应

- `200 OK`: 成功返回文件
- `204 No Content`: CORS 预检请求
- `401 Unauthorized`: 未认证
- `403 Forbidden`: 禁止访问
- `404 Not Found`: 文件不存在
- `429 Too Many Requests`: 超过速率限制

## 文件名验证

文件名必须符合以下格式：

```
- 长度：3-64 字符
- 字符：字母、数字、下划线、连字符
- 扩展名：mp4, mkv, webm, mp3, m4a, flac, png, jpg, jpeg, pdf, zip, docx, txt
```

示例：`video_demo.mp4`, `my_image.jpg`, `document.pdf`

## 性能优化

- AWS 客户端缓存：避免重复创建连接
- 速率限制自动清理：防止内存泄漏
- Range 请求重试：确保大文件流媒体正常
- 热门文件预加载：提前缓存热点内容

## 许可证

MIT License
